## Short Responce ##

1. They both need to make a branch this is important because they both need their own branch to do their own work in it while not affecting ther main.

2. Developers use braches to work on a project without messing up main.

3. Git is a tool that allows us to interact with other repos its a distributed version contol system.Github is a cloud base servise that allows developers to store, track, manage and control chnages to their code. They both work together because github is what developers use to track, manage, and control change in there code. While git doesn't

4. they did git-commit first insted of git-add, it should be..

` git add .`

` git commit -m "saving work"`

` git push`